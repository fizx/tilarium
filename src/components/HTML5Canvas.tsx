import React from "react";

export const HTML5Canvas = () => {
  return (
    <canvas
      style={{
        width: "100%",
        height: "100%",
        position: "absolute",
        top: 0,
        left: 0,
        backgroundColor: "rgba(0, 0, 255, 0.1)", // Placeholder color
      }}
    />
  );
};
